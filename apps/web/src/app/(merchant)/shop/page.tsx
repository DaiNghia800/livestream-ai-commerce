import { MerchantDashboard } from "@/features/dashboard/merchant-dashboard";
export const metadata = { title: "Tổng quan chủ shop" };
export default function Page() {
  return <MerchantDashboard />;
}
